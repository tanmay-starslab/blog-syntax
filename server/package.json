{
  "name": "blog-syntax-server",
  "private": true,
  "version": "0.2.0",
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.25.11",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "tsc -p ./tsconfig.json --noEmit && node ./esbuild.js"
  }
}